# Database Driver Configuration: mysql or sqlite
DB_DRIVER=mysql

# MySQL Settings
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=task_manager
DB_USER=root
DB_PASS=your_secure_password

# SQLite Settings (used when DB_DRIVER=sqlite)
DB_PATH=database.sqlite
